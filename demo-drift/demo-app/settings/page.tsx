// settings/page.tsx
// DELIBERATELY BROKEN: Route not documented in AppFlow.md + missing states

export default function SettingsPage() {
  return (
    <div>
      <h1>Settings</h1>
      <p>User settings page — not in AppFlow.md spec</p>
    </div>
  );
}
